mutation CreateDonation(
  $description: String!
  $value: Float!
  $donatorsName: String!
) {
  createDonnation(
    data: {
      description: $description
      value: $value
      donatorsName: $donatorsName
    }
  ) {
    id
    description
  }
}
